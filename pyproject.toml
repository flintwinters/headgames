[project]
name = "headgames"
version = "0.1.0"
description = "Verification tooling for a minimal analog EEG sonification MVP"
requires-python = ">=3.11"
dependencies = [
    "rich>=13.7",
    "typer>=0.12",
]

[tool.uv]
package = false
